{
  "name": "playora",
  "private": true,
  "version": "1.0.0",
  "description": "Rush X (rush-x.xyz) single-file web build plus bundle patch tooling",
  "scripts": {
    "bundle:split": "python3 tools/bundle.py split",
    "bundle:build": "python3 tools/bundle.py build",
    "smoke": "node tools/smoke.mjs"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
